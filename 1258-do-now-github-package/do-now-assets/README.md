# Do Now assets (1258)

One folder per Do Now, named `dn-<number>-<title>`. Each folder contains the participant README, links, board-item TSVs, solution/sample where applicable, and generated sample images for activities that need a visual.

Start with `manifest.tsv`. Authoritative text is in each folder's README/TSV files; PNGs are generated samples for class use.

"""Pass-7 speaker-notes rewrite for 1258-Ch04-ModernGenAI-PromptEng.pptx (58 slides).

Rebuilds the deck from decks/_bak7/ and replaces every note with the delivery
structure HOOK / KEY POINT / TIME / TRANSITION, preserving [flex] prefixes,
Source citations, prompting-spine lines, and author/IG flags (see notes7_common).
Usage: .venv-courseware/bin/python tools/build_notes_ch04.py
"""
from notes7_common import build, verify

DECK = "1258-Ch04-ModernGenAI-PromptEng.pptx"

T = "(~2 min)"
SPEC = {
1: dict(hook="ASK: Quick show of hands — who used a GenAI tool for real work this week?",
        key="This morning (Day 2 AM): the 2026 model landscape, then the prompting, reasoning, and evaluation skills that make those models dependable for government work. If the half-day runs long, the [flex] slides are the valve — depth, not core message.",
        time="(~1 min)",
        transition="Here's what you'll be able to do by the end of the morning."),
2: dict(hook="SAY (scenario): A colleague pastes a whole policy memo into a chatbot and trusts the fluent answer. This chapter is how you handle that task properly.",
        key="Five objectives: navigate the model landscape, apply prompt techniques, use reasoning deliberately, produce structured outputs, and evaluate with rubrics and verification habits.",
        time="(~1 min)",
        transition="The road map for the morning."),
3: dict(hook="SAY (fact): More than half of this morning is hands-on — a demo, four Do Nows, and two 30-minute labs.",
        key="Orient the room: landscape first, then prompting fundamentals, reasoning models, output control, and evaluation.",
        time="(~1 min)",
        transition="Exactly when you'll be practicing, next slide."),
4: dict(hook="ASK: Notice the pattern in the activity list — short solo Do Nows, then two full labs. Who has done one of our Do Nows already?",
        key="Point out Lab 4.1 (prompting studio with a rubric) and Lab 4.2 (build a template library) — 4.2 is the [flex] item if time gets tight. Full instructions are in the Lab Manual.",
        time="(~1 min)",
        transition="First, a level-set: what generative AI actually is."),
5: dict(hook="SAY (scenario): Traditional AI flags the suspicious benefits claim; generative AI drafts the letter explaining the decision — same agency, two different jobs.",
        key="GenAI creates new content (text, images, audio, video) by learning patterns in data; creation vs. classification/prediction is the dividing line.",
        time=T,
        transition="So what's inside the box that makes creation possible?"),
6: dict(hook="SAY (fact): Everything a model writes is produced one token at a time — each word a prediction of what most likely comes next.",
        key="Transformers plus training data drive the loop: prompt, tokenize, predict, generate; context retention and feedback make iteration coherent.",
        time=T,
        transition="Which models are you actually choosing among in 2026?"),
7: dict(hook="ASK: How many model families does your agency currently have access to — and can anyone name them?",
        key="Four families matter: OpenAI GPT-5.6, Anthropic Claude, Google Gemini, and open-weight Llama 4. Names churn every few months; the categories endure.",
        time=T,
        transition="Here's the one slide worth photographing — the cheat sheet."),
8: dict(hook="SAY (fact): Every frontier family now ships a ~1M-token context window and a reasoning-effort control — table stakes as of August 2026.",
        key="This is the pinned, dated reference. Learn the pattern — flagship / balanced / cost-sensitive / open-weight — not just this month's names.",
        time=T,
        transition="The bigger decision isn't which model — it's where the model runs."),
9: dict(hook="SAY (scenario): A sensitive case file cannot leave your boundary — that single fact decides proprietary-API versus self-hosted open-weight before benchmarks enter the conversation.",
        key="For sensitive data, where the model runs matters more than which model: FedRAMP'd Azure OpenAI/Bedrock, or open-weight on-prem for the most sensitive workloads.",
        time=T,
        transition="Where it runs usually comes down to which tier you bought."),
10: dict(hook="ASK: Who has used a free web tier for a work task? What does the tier actually decide?",
        key="The tier determines what data you may enter — and your context size. Enterprise/government tiers carry the data-protection agreements, admin controls, and FedRAMP status.",
        time=T,
        transition="So which options are actually approved for government right now?"),
11: dict(hook="SAY (fact): GSA's OneGov deal offered ChatGPT Enterprise government-wide for $1 per agency for a year — procurement is no longer the bottleneck; policy is.",
        key="FedRAMP-certified options exist (ChatGPT Enterprise, Gemini for Government, Perplexity for Government); Copilot keeps prompts in-tenant; the approved tier still governs what data you enter.",
        time=T,
        transition="One specification shapes daily use more than any other — the context window."),
12: dict(hook="SAY (scenario): Pasting a 200-page policy into the prompt is now realistic — but every page adds cost and latency, and attention can thin over very long inputs.",
        key="1M-token windows are the 2026 frontier norm; when the corpus exceeds the window, you retrieve the relevant parts instead — that's RAG, coming in Chapter 7.",
        time=T,
        transition="Before adopting any model, read its résumé — the model card."),
13: dict(hook="ASK: When your agency last adopted software, who read the documentation first? A model card is exactly that, for an AI model.",
        key="Cards document intended use, training data at a high level, limitations, and evaluations; for government use add data-handling and FedRAMP posture — this is procurement due diligence.",
        time=T,
        transition="Now the skill that runs the rest of the course: prompt engineering."),
14: dict(hook="SAY (scenario): Treat the model as a brilliant new hire on day one: capable, fast, and completely unaware of your conventions until you spell them out.",
        key="Prompt engineering is designing inputs so the model delivers what you actually wanted at inference time — prompts can be text, images, or a combination.",
        time=T,
        transition="What goes into a well-built prompt? Four elements."),
15: dict(hook="SAY (fact): Most failed prompts are missing one of four things: instruction, context, input data, or an output indicator.",
        key="The prompt is the blueprint — the more precisely you specify each element, the closer the build matches your intent.",
        time=T,
        transition="The two elements that carry the most weight: instructions and context."),
16: dict(hook="ASK: What's the vaguest instruction you've given a model recently — and what did it hand back?",
        key="Clear, specific instructions aligned to the outcome, plus context (background, examples, prior conversation) are what move output quality.",
        time=T,
        transition="Context can do something surprising — teach the model a new task on the spot."),
17: dict(hook="SAY (fact): A model can pick up a brand-new task from two or three examples inside the prompt — no retraining required.",
        key="In-context learning: one-shot and few-shot examples give reference points; most powerful when the output needs a specific format or style.",
        time=T,
        transition="So how do you actually craft one of these prompts?"),
18: dict(hook="SAY (scenario): 'Summarize this' versus 'Summarize this acquisition memo in five bullets for a non-technical executive' — same model, wildly different results.",
        key="Clear, concise, purpose-driven language; balance information density with clarity — word choice and format both steer the response.",
        time=T,
        transition="Why does this skill deserve a whole chapter? The payoff is next."),
19: dict(hook="SAY (fact): GSA's own evaluation of its internal GenAI chat found 82% of users never changed the default model and a median of just 6 prompts per user — this chapter teaches the habits the 82% never practice.",
        key="Prompting buys control, user experience, and flexibility — leverage on an AI investment your agency has already made.",
        time=T,
        transition="Where does that leverage apply? Three arenas."),
20: dict(hook="ASK: Which of these three — expertise, critical thinking, or creativity — would save your office the most hours?",
        key="Prompting focuses vast general knowledge into specialized, actionable output: domain expertise, structured analysis, and creative generation.",
        time=T,
        transition="Let's get concrete: the four prompt types."),
21: dict(hook="SAY (fact): One taxonomy, four rungs: zero-shot, one-shot, few-shot, chain-of-thought — each buys capability at the price of prompt length.",
        key="Match the type to the task: simple task → zero-shot; a style to mimic → one-shot; a pattern to learn → few-shot; reasoning to expose → CoT.",
        time=T,
        transition="Take them one at a time, starting with the one you already use."),
22: dict(hook="SAY (scenario): 'List the top 5 tourist attractions in Paris' — no examples, no setup; the model leans entirely on its pre-trained knowledge.",
        key="Zero-shot is quick to deploy and right for straightforward tasks; it trades some accuracy and complexity for convenience.",
        time=T,
        transition="Give it one example and the behavior changes noticeably."),
23: dict(hook="SAY (fact): A single worked example can set the tone and structure of every answer that follows.",
        key="One-shot suits repeatable patterns — it bridges no-context zero-shot and full few-shot, and it beats zero-shot on slightly complex requests.",
        time=T,
        transition="If one example helps, what do three or four do — and where can that go wrong?"),
24: dict(hook="SAY (scenario): A team needs sentiment labels on a new product's feedback with zero annotated data — three labeled examples in the prompt bootstrap the whole classifier.",
        key="Few-shot (2+ examples) establishes a pattern; ideal for complex tasks or required formats, and resource-efficient when labeled data is scarce.",
        time=T,
        transition="But examples can teach the wrong lesson — next slide."),
25: dict(hook="SAY (fact): Label one positive review negative in your examples and the model will faithfully learn your mistake.",
        key="In-context learning is only as good as its examples: misleading, conflicting, or biased examples produce unreliable outputs — double-check every prompt-completion pair.",
        time=T,
        transition="Here's the safe workflow for using examples."),
26: dict(hook="ASK: If examples aren't working, what's your next move — more examples, or a different approach?",
        key="Ladder up: zero-shot baseline, then one-shot, then few-shot with relevant, consistent examples; mind the context window; if examples still fail, consider a different model or fine-tuning (later chapters).",
        time=T,
        transition="The fourth prompt type changed the field — chain-of-thought."),
27: dict(hook="SAY (fact): 'Let's think step by step' — five words that measurably improve reasoning on multi-step problems.",
        key="CoT asks the model to reveal its reasoning sequentially — vital when the rationale matters as much as the answer. Caveat: a visible chain is not proof of correctness; NIST AI 600-1 documents confabulated logic and citations.",
        time=T,
        transition="Watch the difference on one arithmetic problem."),
28: dict(hook="ASK: Read the two responses — which one would you rather defend in an audit?",
        key="Same question, same answer (3 apples) — but the CoT version exposes each step, so errors become findable and the answer becomes reviewable.",
        time=T,
        transition="Why that visibility matters beyond arithmetic."),
29: dict(hook="SAY (scenario): An auditor asks 'why did the system say no?' — a visible reasoning chain is the difference between an answer and a defensible answer.",
        key="CoT improves accuracy on reasoning tasks, adds transparency, and makes debugging possible: a faulty chain shows you exactly where understanding broke.",
        time=T,
        transition="That manual trick has now been absorbed into the models themselves."),
30: dict(hook="SAY (fact): Reasoning is no longer a separate product line — it's a built-in mode of the main model families, dialed with an effort setting.",
        key="The CoT slides just before this are now historical technique: OpenAI reasoning.effort, Claude adaptive thinking, and Gemini thinking_level all do the reasoning for you.",
        time=T,
        transition="What are 'thinking' models actually doing with that extra computation?"),
31: dict(hook="SAY (scenario): A multi-criteria eligibility determination benefits from reasoning effort; a routine form-letter draft does not — same toolbox, different settings.",
        key="Thinking models spend more computation before answering — exploring and checking their own steps; strong on multi-step reasoning, unnecessary for simple lookups and drafting.",
        time=T,
        transition="Does manual step-by-step still earn its place?"),
32: dict(hook="SAY (fact): Even with reasoning models, asking for structured steps buys transparency — and for auditable government decisions, the visible rationale matters as much as the answer.",
        key="Match the technique to the model and to the accountability need: reasoning modes reduce but don't eliminate the value of explicit 'show your work' prompts.",
        time=T,
        transition="Effort isn't free — let's price it."),
33: dict(hook="ASK: Who pays when someone runs every routine query at maximum effort? Your token budget does.",
        key="Reasoning costs more and responds slower; reserve high effort for hard, high-stakes tasks — Lab 7.1's token/cost printout will show exactly what you spend.",
        time=T,
        transition="One knob, three vendor names — the reference card."),
34: dict(flex_msg="match the effort setting to the stakes — the previous slide carries that core message",
        hook="SAY (fact): Reasoning tokens are billed as output tokens on all three platforms — effort is literally money (Sol $5/$30 vs. Luna $1/$6 per million tokens).",
        key="The same knob, three labels: OpenAI reasoning.effort (none→max), Anthropic's effort parameter, Gemini thinking_level; eligibility analysis = high, form letter = low or none.",
        time=T,
        transition="Beyond the basics sits a shelf of advanced techniques."),
35: dict(hook="SAY (scenario): Socrates taught by asking 'why' until the logic exposed itself — maieutic prompting does the same thing to a model.",
        key="Chain-of-thought decomposes problems; tree-of-thought explores branches and alternatives; maieutic interrogates the model's own reasoning to expose inconsistencies.",
        time=T,
        transition="Three more techniques aimed at complexity and accuracy."),
36: dict(hook="SAY (fact): 'Do your homework first' is a real technique — generated-knowledge prompting makes the model gather facts before it answers.",
        key="Complexity-based prompting prioritizes the most complete answer; generated-knowledge fronts the fact-gathering; least-to-most chains small steps — for research-heavy, sequenced work.",
        time=T,
        transition="Getting a draft is step one; refining it is the next pair of techniques."),
37: dict(hook="ASK: Who has told a model 'that's not quite right, try again'? You were already using self-refine prompting.",
        key="Self-refine has the model critique and improve its own output; directional-stimulus nudges with hints and keywords — polish and steer.",
        time=T,
        transition="Technique is half the craft — the other half is clarity habits."),
38: dict(hook="SAY (fact): 'Tell me about dogs' versus 'a short paragraph on a Golden Retriever's physical traits' — specificity is the whole game.",
        key="Define the desired outcome precisely, supply anchoring context (keywords, examples, data), and stay concise — vague phrasing invites misinterpretation.",
        time=T,
        transition="Clarity also lives in the format you ask for."),
39: dict(hook="SAY (scenario): 'A 5-point bullet list of pros and cons, under 150 words, formal tone' — the model's workspace is organized before it writes a word.",
        key="Specify the output format (list, paragraph, table, code) and constraints (length, tone, vocabulary) to get immediately usable output instead of sprawl.",
        time=T,
        transition="Even a great first prompt is a first draft — iteration is the art."),
40: dict(hook="ASK: How many iterations does a good prompt usually take you — one, three, ten?",
        key="Start simple, test variations, evaluate the output critically, refine — prompt engineering is a conversation, not a coin toss.",
        time=T,
        transition="Next, giving the model a job title — role and system prompting."),
41: dict(hook="SAY (scenario): 'You are a FOIA officer. Cite the exemption for every redaction.' One sentence in the system prompt shapes every answer in the conversation.",
        key="The system prompt sets role, rules, and boundaries for the whole session; roles beat restating instructions each message; in code (Chapter 7) it's the first message you send.",
        time=T,
        transition="Role controls behavior; the next slide controls shape."),
42: dict(hook="SAY (fact): Format control is the difference between a demo and a usable work product.",
        key="Ask for exactly the shape you need (table, numbered list, one-paragraph summary, JSON), state length and audience, and give a template to fill — less cleanup afterwards.",
        time=T,
        transition="Three structured formats earn special attention."),
43: dict(hook="SAY (scenario): Feed a memo in, get JSON out, and the result flows straight into the next system — no copy-paste cleanup.",
        key="JSON when output feeds another system, a Markdown table when a person compares items, Mermaid for diagrams from text — structure turns chat into a workflow component.",
        time=T,
        transition="That used to be a polite request; now it's a platform guarantee."),
44: dict(hook="SAY (fact): All three major platforms now guarantee schema-valid JSON — but schema-valid is not factually correct; you still validate the values.",
        key="OpenAI Structured Outputs (strict JSON Schema) plus Claude and Gemini equivalents; handle refusals and token-limit cutoffs as normal failure modes — this is what makes Lab 7.1's pipeline possible.",
        time=T,
        transition="The strongest accuracy tool costs nothing: grounding."),
45: dict(hook="ASK: What if you could forbid the model from using anything outside your document?",
        key="'Answer only from the text above; if it is not there, say so' — grounding cuts hallucination and produces citable answers; it's RAG in miniature (Chapter 7 automates it).",
        time=T,
        transition="Let's assemble everything into one prompt contract."),
46: dict(hook="SAY (fact): A good prompt is a small specification, not a wish.",
        key="Combine role + task + context + format + constraints into one reusable prompt — then save your best contracts as templates.",
        time=T,
        transition="Templates are exactly where we go next."),
47: dict(hook="SAY (scenario): A comms team needs twenty content variations by noon — one template with [BRACKETED] slots beats twenty blank prompts.",
        key="Templates pair clear task instructions with placeholders positioned where inputs vary — instruction clarity determines output quality.",
        time=T,
        transition="Two templates you can adapt today."),
48: dict(hook="ASK: Read the customer-support template — which [slots] would your office fill in most often?",
        key="Walk both examples: role-plus-issue structure for support responses; tone/topic/word-count slots for content generation — consistency is the payoff.",
        time=T,
        transition="Why templates multiply productivity across a whole team."),
49: dict(hook="SAY (fact): A shared template library is how one person's prompting skill becomes the whole team's standard output.",
        key="Templates deliver consistency, collaboration, context customization, accessibility, and task-specific optimization — make them easy to find across the organization.",
        time=T,
        transition="Where templates are already earning their keep."),
50: dict(hook="SAY (scenario): E-commerce listings, support replies, blog posts, translations, code documentation — five places one template each saves hours every week.",
        key="Templates standardize quality wherever content is repetitive; the same pattern applies to your recurring government documents.",
        time=T,
        transition="Output control done — now the hard question: when the output is wrong."),
51: dict(hook="ASK: Whose assumptions are baked into a model trained on the internet's text?",
        key="Bias enters through training data and through your prompts — watch for gendered language in drafts and cultural insensitivity in multilingual output; audits and refinement are the mitigations (Chapter 5 goes deep).",
        time=T,
        transition="Bias is subtle; fabrication is brazen."),
52: dict(hook="SAY (fact): NIST gave this failure mode its own name — confabulation: confidently stated false content, including invented logic and citations that make wrong answers look justified.",
        key="Plausible is not accurate; mitigation is fact-checking workflows plus trained user validation — human oversight on anything public-facing.",
        time=T,
        transition="So evaluate every output — here's how to do it systematically."),
53: dict(hook="SAY (scenario): The most dangerous AI output is the fluent, well-formatted, wrong one — it passes every skim.",
        key="Define 'good' before you prompt (accuracy, completeness, format, tone), then score against that rubric — Lab 4.1 uses a 1–5 rubric for exactly this.",
        time=T,
        transition="Rubrics scale — even to a second model acting as judge."),
54: dict(hook="SAY (fact): You can ask a second model to score outputs against your rubric — useful at scale, but a human keeps the consequential calls.",
        key="A rubric turns 'better' into measurable 1–5 criteria; LLM-as-judge compares prompt versions or models systematically.",
        time=T,
        transition="Evaluation habits, tuned for government work."),
55: dict(hook="ASK: If an AI-drafted number ends up in a report to leadership, whose name is on the error?",
        key="Check facts against authoritative sources, require citations for published claims, keep records (AI-assisted products may be federal records) — you, not the tool, are accountable.",
        time=T,
        transition="Time to practice all of it — Lab 4.1."),
56: dict(hook="SAY (scenario): Take a real document, prompt it plainly, then climb the ladder: zero-shot → few-shot → role + format contract → reasoning-model comparison — scoring each rung against the rubric.",
        key="Lab 4.1: Prompt Engineering Studio — improve real outputs through iteration against a scoring rubric; Lab Manual has the full steps.",
        time="30 min activity — circulate and coach teams up the ladder; note which rung bought each team the most quality; debrief when to stop iterating and switch approach (bridge to Chapter 7).",
        transition="Then we bottle the winning prompt as a reusable template in Lab 4.2."),
57: dict(force_flex=True,
        flex_msg="a prompt that works once is a trick; a template that works on unseen inputs is a tool",
        hook="ASK: What's the one prompt from Lab 4.1 you would hand a colleague tomorrow?",
        key="Lab 4.2: Build a Reusable Prompt Template Library — replace the specifics with [BRACKETED] slots and write it as a Python function taking those slots as arguments.",
        time="30 min activity — circulate and push participants to generalize the slots; preview that these templates feed Chapter 7's code work. Data: federal_ai_use_cases.csv; uses the OpenAI key from .env.",
        transition="Let's wrap the morning."),
58: dict(hook="SAY (fact): You now hold the four habits most users never build: landscape awareness, specification prompting, effort-matched reasoning, and rubric-based verification.",
        key="Read the takeaways and the closing thought; next steps — complete Lab 4.1, save your best prompts as templates, and identify one content-heavy workflow where grounded, verified prompting could help.",
        time=T,
        transition="After lunch: what happens when AI itself is the target — security, risks, and responsible AI."),
}

if __name__ == "__main__":
    out = build(DECK, SPEC)
    problems = verify(DECK, expected_count=58)
    print(f"built {out}")
    print("verify:", "OK" if not problems else "\n  " + "\n  ".join(problems))

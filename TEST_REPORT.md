# Course 1258 lab test report

Date: 2026-08-11
Scope: `/home/student/1258-new/labs`
Mode: offline / no API key (`OPENAI_API_KEY=""`), using the project venv.
Change under test: **non-programmer-friendly rewrite of all 16 labs** — student
notebooks now make one plain-English `lab_helpers.py` call per step and edit
only simple values in pre-filled `# YOUR TURN` cells; the original
code-forward labs are archived in `labs/For_Python_Programmers/`.

## Commands run

```bash
cd /home/student/1258-new/labs
OPENAI_API_KEY= bash build_notebooks.sh

cd /home/student/1258-new
OPENAI_API_KEY= .venv/bin/python tools/validate_release.py
.venv/bin/python tools/check_friendly.py

# archive self-containment (from labs/For_Python_Programmers/):
OPENAI_API_KEY= for each of the 16 archived notebooks: nbconvert --execute --inplace
```

Plus a consolidated `nbformat` output comparison of every new executed student
notebook against its archived executed original.

Environment used by the test:

- Python: `3.12.13` (`/home/student/1258-new/.venv/bin/python`)
- JupyterLab: `4.6.2`; Jupytext: `1.19.5`; kernelspec `1258-a4` registered
- The working tree was dirty before the run (generated `.ipynb` differ from
  the last commit until rebuilt); the run regenerates them from `labs/src/*.py`.

## Result summary

| Check | Result |
|---|---|
| Jupytext conversion `src/*.py -> .ipynb` | PASS: 31 notebooks converted |
| Student notebook offline execution | PASS: 16/16 |
| Solution notebook offline execution | PASS: 15/15 |
| Solution transcripts regenerated | PASS: 15 written |
| `tools/validate_release.py` release gate | PASS: all checks |
| `tools/check_friendly.py` friendly-lab guardrail | PASS: all checks |
| Archived originals offline execution (in `For_Python_Programmers/`) | PASS: 16/16 |
| Functional equivalence vs archived outputs | PASS (see below) |
| Hard-coded keys / retired API in code cells | none found |

Wall-clock: full build + smoke run about 3m 31s.

## The friendly rewrite in numbers

`tools/check_friendly.py` counts visible (non-comment) lines of code in
student notebook cells, before (archived originals) vs after:

| metric | value |
|---|---:|
| Visible lines before | 1,626 |
| Visible lines after | 191 |
| Reduction | **−88%** |
| Per-lab range | −71% (4.2) to −94% (7.3) |
| Student cells containing imports/`def`/`try`/loops/comprehensions/`json.`/`re.`/pandas/sklearn/matplotlib/OpenAI SDK/assert/f-string prints | 0 (guardrail-enforced) |
| `# YOUR TURN` cells without a pre-filled working value | 0 (guardrail-enforced) |

What students now see per lab: one `from lab_helpers import *` setup cell
(plus `lab_status()` in the API labs), one friendly call per step
(`epa = load_epa_data()`, `elbow_plot(X)`, `run_agent_task(AGENT_REQUEST)`),
and `# YOUR TURN` value edits (prompt text, `K = 4`, `KEYWORD = "air quality"`,
`STATE = "California"`, …). Markdown write-in work (cluster names, briefings,
reflections) is unchanged. Every student notebook ends with a pointer to the
code-forward version in `For_Python_Programmers/`.

## Functional equivalence

Each rewritten lab was diffed against its archived executed original during
the rewrite (per-lab ordered output diffs), then re-checked consolidated after
the final full build: every dataset row count, cluster statistic, inertia
value, quality measurement, PII hit count, canned reply, extracted JSON field,
token/cost figure, retrieval score, and agent-trace line from the archived
outputs is reproduced in the new outputs. Notable spot checks:

- **1.2 clustering** — k=3 sizes 842/152/3, inertia series (k=4: 2,603), worst
  cluster id, worst-county table: identical.
- **5.2 PII** — `{ssn 5, email 6, phone 5}`, masked-record JSON byte-identical;
  Presidio/regex fallback path unchanged (`lab_common.mask_pii`).
- **6.1 data quality** — all measurements identical; student scorecard
  byte-identical.
- **7.1 API** — canned FOIA/persona replies, memo JSON (all 5 key_rules),
  631/174/805 tokens, $0.20/1,000-call cost: identical.
- **7.2 RAG** — 26 chunks, retrieval scores, grounded/ungrounded canned
  answers: identical; local-embedding fallback untouched.
- **7.3 agent** — canned traces incl. the deny-by-default approval-gate line;
  outbox no-SSN assertion intact (`lab_common.confirm` semantics unchanged).
- **8.1 visualization** — `epa_briefing.png` regenerated byte-identical (md5
  match).

The only archived output lines not reproduced are the old
`(reference … applied — replace with your own …)` fallback messages, removed
by design: `# YOUR TURN` cells are now pre-filled with the reference value
instead of shipping a `None` + fallback branch.

## Archive

`labs/For_Python_Programmers/` holds the frozen code-forward snapshot: the 16
original student notebooks, 15 solution notebooks + transcripts, the original
jupytext `src/`, its own `lab_common.py`, a `data -> ../data` symlink, and a
README. All 16 archived student notebooks re-executed offline 16/16 PASS from
inside the folder. `build_notebooks.sh` globs only `labs/src/*.py`, so the
archive is never rebuilt.

## Docs updated

- `README.md` — two-audience design, `lab_helpers.py` /
  `For_Python_Programmers/` in the repo map, `check_friendly.py` in the
  cross-cutting design notes.
- `labs/README.md` — friendly-lab contract, contents table rows for
  `lab_helpers.py` and the archive, `# YOUR TURN` wording.

## Notes / follow-ups (not blocking)

1. The workbook lab pages (`workbook/1258-WBa4-Lab-Manual.md`) reference labs
   by filename and step, so they still line up, but a few step wordings assume
   visible code (e.g. Lab 7.3 Stage A "call each provided tool directly once",
   Lab 1.2 "Select the numeric day-count columns"). Consider a workbook wording
   pass to match the guided notebooks.
2. The 12 pre-existing optimization suggestions from the 2026-08-07 report
   (cached-first data.gov, Presidio engine caching, FIPS-friendly hash,
   machine-readable smoke output, etc.) remain valid and unaddressed — they
   were out of scope for this rewrite.
3. `registry/labs.yaml` is unchanged by design (artifact filenames identical);
   the registry remains the source of truth.

## Bottom line

The lab pack now serves both audiences: non-programmers get guided notebooks
with 88% less visible Python and nothing they can break (every cell runs as
shipped), programmers keep the full code-forward versions in
`For_Python_Programmers/`. All gates pass: 31/31 notebooks execute offline,
the release gate is green, the friendly-lab guardrail is green, the archive
re-executes 16/16, and outputs are functionally identical to the originals.
